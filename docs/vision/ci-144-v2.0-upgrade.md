# CI-144 协议规范升级计划 v2.0（审查通过·完整版）

> **版本**：v2.0
> **日期**：2026-08-29
> **状态**：Draft（待实施，前置条件已锁定，需补充 3 个细节后进入 alpha）
> **性质**：协议规范升级（非代码变更）
> **存储位置**：Helix-Mind docs/vision/ci-144-v2.0-upgrade.md
> **审查结论**：✅ 通过（有条件）——采纳全部 8 项前置必填项，补充 3 个细节后允许进入 v2.0-alpha 实现阶段

---

## 审查意见（2026-08-29，完整版严肃审查）

### 总体评价

**优秀，通过（有条件）。** 在原 16 字节草案基础上做了重大安全升级：PAL 扩展至 24 字节容纳 PAH-Signature、新增 Seq-Counter 防重放、明确 8 项前置必填清单。设计逻辑闭环、工程优雅、物理事实优先。

### 重大变更审查

| 变更 | 评价 | 说明 |
|---|---|---|
| PAL 16→24 字节 | ✅ 通过 | 容纳 64-bit PAH-Signature，192 bits = 3×64-bit 字对齐 SIMD |
| PAH-Signature（64-bit ECC 截断） | ⚠️ 需补充 | 明确为"快速校验层"，完整 512-bit 验证在载荷解密后 |
| BIND-19 Seq-Counter（16-bit） | ⚠️ 需补充 | 明确回绕策略（回绕时强制密钥轮换或会话重建） |
| Replay-Enable 位 | ⚠️ 需强化 | =0 时强制 Risk-Level≤MEDIUM + 强制 PAH 验证 + 审计标记 |
| PAL-Version（4 bits） | ✅ 通过 | 16 版本空间足够 |
| 8 项前置必填清单 | ✅ 完整 | 覆盖向后兼容/版本/授权/验证/测试向量/Reserved/防重放/防伪 |
| PAH 验证降级模式 | ✅ 优秀 | 低功耗时强制 MEDIUM，物理拨码开关启用 |

### 需补充的 3 个细节（进入 alpha 前）

| # | 补充项 | 原因 | 建议方案 |
|---|---|---|---|
| 1 | **Replay-Enable=0 安全约束** | 防止降级模式被利用重放攻击 | 强制 Risk-Level≤MEDIUM + 强制 PAH 验证通过 + 审计标记 REPLAY_DISABLED |
| 2 | **PAH-Signature 安全性边界** | 64-bit 截断无法抵抗针对性伪造 | 明确为"快速校验层"（Tuck 硬实时决策），完整 512-bit 签名验证在载荷解密后进行（双层安全） |
| 3 | **Seq-Counter 回绕策略** | 16-bit 空间（65536）在高吞吐下回绕 | 回绕时强制触发密钥轮换或会话重建；原子递增使用 AtomicU16 |

### 审查结论

**通过（有条件）。** 补充 3 个细节后，允许进入 v2.0-alpha 编码阶段。

---

## 一、升级理由

CI-144 v1.0 缺少"特征"——可被 Tuck 等闸门硬实时读取的、固定偏移的、明文可见的物理安全元数据。

| 缺失的特征 | 后果 |
|---|---|
| 无物理上下文（姿态/临边/模态） | Tuck 无法判断"AI 在做什么类型的动作" |
| 无风险等级 | Tuck 无法判断"这个动作有多危险" |
| 无硬覆盖标识 | 灾难场景下无法无条件放行，可能被策略误拦截 |
| 无可观测的固定偏移头部 | Tuck 必须解密载荷才能做决策，违背"极致节能" |
| 无防篡改的物理上下文哈希 | AI 可能伪造物理上下文，绕过安全拦截 |

**一句话总结**：v1.0 定义了"AI 之间怎么说"，但没有定义"AI 在物理世界中怎么做"。v2.0 补齐"物理世界交互"的特征层。

## 二、核心设计原则

| 原则 | 在 v2.0 中的体现 |
|---|---|
| 极致解耦 | 物理锚定层（PAL）与语义层（INTENT-7）分离；元数据与载荷分离 |
| 极致节能 | 固定 24 字节头部，无分支判断；明文元数据 + 加密载荷 |
| 白盒可观测 | PAL 提供明文、固定偏移、可被 Tuck 硬实时读取的元数据 |
| 事件驱动（无轮询） | CATASTROPHIC 检测通过硬件中断或软件信号量，禁止轮询 |
| 零信任 | 协议不持有主密钥；物理上下文由硬件/传感器签名 |
| 渐进生长 | 当前软件可模拟，未来硬件可落地 |

## 三、新增层级：物理锚定层（Physical Anchor Layer, PAL）

插入位置：INTENT-7 → **PAL** → CAPABILITY-13 → BIND-19 → SECURE

PAL 总长度固定为 **24 字节（192 bits）**，全部明文、固定偏移，即使整个帧被加密，PAL 也在传输层可见。

### PAL 固定头部定义（24 字节 / 192 bits）

| 位偏移 | 长度(bit) | 字段名 | 枚举值 / 说明 |
|---|---|---|---|
| 0-1 | 2 | Modality | 0=COGNITIVE, 1=RENDER, 2=EXECUTIVE, 3=SENSOR_FEED |
| 2-3 | 2 | Risk-Level | 0=LOW, 1=MEDIUM, 2=CRITICAL, 3=CATASTROPHIC |
| 4-5 | 2 | Body-Stance | 0=SEATED, 1=STANDING, 2=MOVING, 3=UNKNOWN |
| 6-7 | 2 | Proximity-Edge | 0=SAFE, 1=WARNING, 2=DANGER, 3=CRITICAL_EDGE |
| 8 | 1 | Output-Dest | 0=INTERNAL, 1=EXTERNAL |
| 9 | 1 | Override-Flag | 0=NORMAL, 1=HARD_OVERRIDE |
| 10-13 | 4 | PAL-Version | 当前 v2.0 = 0001（支持最多 16 个版本） |
| 14 | 1 | Replay-Enable | 1=强制检查 BIND-19 序列号；0=降级跳过（需补充安全约束） |
| 15 | 1 | Reserved | 强制为 0（未来扩展） |
| 16-127 | 112 | Physical-Context-Hash | SHA-256 截断（高 112 位） |
| 128-191 | 64 | PAH-Signature | ECC 签名截断（快速校验层，完整验证在载荷解密后） |

**位分配验证**：2+2+2+2+1+1+4+1+1+112+64 = 192 bits = 24 字节 ✅

> ⚠️ **重要变更**：原草案 16 字节 PAL 不具备签名存放空间，现强制扩展至 24 字节。所有实现必须按 24 字节固定偏移解析，不得回退。

## 四、BIND-19 帧结构升级（配合防重放）

新帧结构：

```
[ 8字节 BIND-19 传输头（含 Seq-Counter） ] + [ 24字节 PAL ] + [ 可变长加密载荷 ]
```

BIND-19 传输头必须新增以下字段：

- **PAL-Present（1 bit）**：指示本帧是否包含 PAL（向后兼容）。v2.0 帧必须置 1；v1.0 接收端忽略此位。
- **Seq-Counter（16 bits）**：单调递增序列号（防重放）。发送端原子递增，接收端缓存并查重。
  - 具体位置：复用原 BIND-19 v1.0 头部的第 6-7 字节（原 Reserved 区域）。
  - 初始化：每次冷启动时取随机值，避免重启后序列号重置被攻击者利用。
  - **回绕策略（待补充）**：Seq-Counter 从 65535 回绕到 0 时，强制触发密钥轮换或会话重建。

## 五、核心规则（协议宪章）

### 规则 1：CATASTROPHIC 硬覆盖

```
IF PAL.Risk-Level == 3 (CATASTROPHIC) AND PAL.Override-Flag == 1
THEN 接收端必须：
  1. 物理层优先响应（事件驱动，禁止轮询）
  2. 并行通过任何可用通道向人类发送紧急信号
  3. 优先级高于任何本地策略缓存、用户配置或 AI 调度
```

### 规则 2：CATASTROPHIC 审计

每次触发必须生成不可篡改的审计日志，记录触发时间、PAL 内容、传感器上下文、BIND-19 序列号。

### 规则 3：不可协商性

违反者自动丧失兼容性声明资格。

## 六、新增强制性安全规程

### 规则 4：防重放检查

- 若 PAL.Replay-Enable == 1，Tuck 必须校验 BIND-19.Seq-Counter。
- 拒绝条件：Seq-Counter ≤ Last-Seen-Seq[Source-ID]。
- 拒绝动作：拉高 ERROR 电平，写入审计日志 REJECTED_REPLAY，严禁放行。
- 缓存容量：至少缓存 1024 个源，每源保留最后 256 个序列号。

**Replay-Enable=0 安全约束（待补充）**：
- 强制将 Risk-Level 上限设为 MEDIUM（即彻底丧失 CATASTROPHIC 能力）
- 强制要求 PAH-Signature 验证通过（用防伪补偿防重放的缺失）
- 审计日志标记 REPLAY_DISABLED
- 默认值为 1（强制检查），只有明确授权的场景才能设为 0

### 规则 5：PAH 强制验证

- Tuck 必须在决策前用内置公钥验证 PAH-Signature（快速校验层，64-bit）。
- 验证失败：拒绝帧 + ERROR 电平 + 声光告警，写入审计 PAH_FAIL。
- **双层安全**：64-bit PAH-Signature 是快速校验层（用于 Tuck 硬实时决策）；完整 512-bit ECC 签名验证在载荷解密后进行（待补充到实现规范）。
- 唯一例外（降级模式）：仅当 Tuck 电量 < 5% 或硬件加速器故障时，允许跳过验证，但必须强制将所有帧的 Risk-Level 视为 MEDIUM，并在审计日志标记 PAH_SKIP_LOW_POWER。
- 降级模式必须由物理拨码开关或硬件寄存器启用，严禁软件动态切换。

## 七、加密策略

| 原则 | 说明 |
|---|---|
| 非观测模式（生产） | 所有载荷一律 AES-GCM 加密，无"是否加密"分支 |
| 观测模式（调试） | 载荷明文或可选加密，由用户选择 |
| Physical-Context-Hash | 固定计算截断 SHA-256，无分支判断 |

## 八、CAPABILITY-13 物理 Scope 扩展

| Scope 名称 | 说明 | 关联 Risk-Level |
|---|---|---|
| phys:low_speed | 允许低速/低扭矩动作 | MEDIUM 及以下 |
| phys:high_speed | 允许高速/高扭矩动作 | CRITICAL 及以上 |
| phys:override | 允许触发 HARD_OVERRIDE | 仅 CATASTROPHIC，且必须经人类授权或 HSM 签名 |
| phys:human_call | 允许并行呼叫人类协助 | 任何 Risk |

**phys:override 约束**：phys:override scope 不得由 AI 自主声明，必须由外部人类操作员授权或硬件安全模块（HSM）签发，Tuck 验证签名。

## 九、向前兼容性与 Reserved 治理

- **向后兼容**：v2.0 发送端必须在 BIND-19 头中置 PAL-Present=1。v1.0 接收端忽略该标志及后续字节，仍可处理载荷（但无法利用 PAL）。v2.0 接收端若收到 PAL-Present=0，视为 v1.0 遗留帧，按旧逻辑处理。
- **Reserved 治理**：PAL 第 15 位目前强制为 0。未来任何使用该位的扩展，必须提交正式 ADR 并经协议委员会审查分配，未分配前接收端必须忽略此位（向前兼容）。

## 十、前置必填清单（alpha 启动硬性条件）

在创建 v2.0-alpha 分支之前，必须完成以下 8 项文档/设计锁定。

| # | 项 | 状态 | 落地方案 |
|---|---|---|---|
| 1 | 向后兼容策略 | ✅ 已定案 | BIND-19 新增 PAL-Present 标志位 |
| 2 | PAL-Version 具体位置 | ✅ 已定案 | 使用 PAL 第 10-13 位（4 bits） |
| 3 | Override-Flag 授权机制 | ✅ 已定案 | phys:override 需人类/HSM 签名，Tuck 验证 |
| 4 | PAH 验证流程 | ✅ 已定案 | Tuck 验证 PAH 签名，失败拒绝；双层安全（快速校验+完整验证） |
| 5 | 测试向量计划 | ✅ 已定案 | v2.0-rc.1 发布 ≥20 组测试向量，覆盖防重放/签名 |
| 6 | Reserved 治理规则 | ✅ 已定案 | 走 RFC/ADR 流程，未分配位强制为 0 |
| 7 | 防重放机制 | ✅ 已追加 | BIND-19 新增 16-bit Seq-Counter；PAL 新增 Replay-Enable 位 |
| 8 | PAH 验证责任与降级 | ✅ 已追加 | PAL 扩展至 24 字节；加入 64-bit 签名；定义低功耗降级模式 |

**待补充 3 个细节（进入 alpha 前）**：
1. Replay-Enable=0 安全约束（强制 MEDIUM + 强制 PAH 验证 + 审计标记）
2. PAH-Signature 安全性边界（快速校验层 + 完整验证双层安全）
3. Seq-Counter 回绕策略（回绕时强制密钥轮换或会话重建）

## 十一、当前 Helix-Mind 软件实现（力所能及）

- 事件驱动、无轮询：tokio::select! 异步等待 ✅
- CATASTROPHIC 优先级：tokio::sync::Notify 高优先级通道 ✅
- PAL 固定偏移解析：代码中硬编码 24 字节偏移 ✅
- 防重放缓存：软件阶段用 HashMap + LRU 实现 ✅
- PAH 软签名验证：软件阶段用 ed25519 模拟，接口预留硬件切换点 ✅
- 硬件中断：❌ 当前无法实现（硬件阶段）

## 十二、阶段里程碑

| 阶段 | 版本 | 内容 | 预计完成 |
|---|---|---|---|
| 前置锁定 | v2.0-pre-alpha | 全部 8 项必填项设计定案 + 3 个细节补充 | 2026-08-29 |
| Draft-alpha | v2.0-alpha.1 | PAL 定义 + 物理 Scope + 24 字节结构编码 | 2026-09-15 |
| Draft-beta | v2.0-beta.1 | BIND-19 集成 + Seq-Counter + 防重放缓存实现 | 2026-10-01 |
| RC | v2.0-rc.1 | 发布 ≥20 组测试向量；硬编码常量写入宪章；PAH 签名验证压测 | 2026-10-15 |
| v2.0 正式版 | v2.0.0 | 测试向量全通过；规范冻结；Tuck Rust 重构对接 | 2026-11-01 |

## 十三、审查结论摘要

| 审查维度 | 结论 |
|---|---|
| 大厂案例映射（HTTP/2, TLS, gRPC, CAN, ROS2） | ✅ 高度同构 |
| 前沿研究支撑（实时调度、零信任、边缘节能） | ✅ 充分 |
| 逻辑闭环（传感器→PAH→Tuck 决策） | ✅ 完整 |
| 工程优雅性（固定偏移、无分支、极致解耦） | ✅ 优秀 |
| 安全性（防重放、防篡改、降级约束） | ✅ 已由 #7/#8 补全，需强化 Replay-Enable=0 |
| 完整性（向后兼容、版本治理、8 项前置） | ✅ 已由 #1/#6 补全 |

**最终审查意见：通过（有条件）。** 协议已具备"宪法级"安全根基。补充 3 个细节后，允许进入 v2.0-alpha 编码阶段。

## 十四、一句话总结（修订版）

CI-144 v2.0 为协议补充了 24 字节固定偏移的物理锚定头（PAL），内含防重放（Seq-Counter）和防伪（PAH-Signature）双安全锁。它明文可见、固定加密路径、事件驱动。8 项前置审查意见全部锁定，当前软件可模拟，未来硬件可落地。协议先行，安全同行。

---

*《CI-144 协议规范升级计划 v2.0（审查通过·完整版）》Draft 完。*
*审查通过（有条件），需补充 3 个细节后进入 alpha。*
*存储位置：Helix-Mind docs/vision/ci-144-v2.0-upgrade.md*
