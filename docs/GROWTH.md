# 变异日志

> **所属方法论**：DNA 自生长方法论 v1.0
> **对齐知识本体**：v4.0

规则：只保留最近 3 条。第 4 条写入时，最旧的归档到 `docs/archive/growth/`。

---

## [2026-08-25] 变异：知识本体 v4.0 — DNA 方法论引入

### 触发条件
单文件白皮书 v3.4 膨胀至约 2 万字，难以在 AI 协作会话中完整加载。

### 变更性质
- **知识本体**：v3.4 → v4.0（硬冻结契约无变更，哲学公理无变更，仅文档架构重组）
- **管理方法**：引入 DNA 自生长方法论 v1.0
- **文件形态**：1 个白皮书 → 5 个根文件 + 12 个 spec 分卷 + 9 个决策卡

### 兼容性
- 所有硬冻结契约（HelixQuery、Node/Edge Schema 等）无变更
- 外部组件（Anaphase v3.4+）无需修改即可对齐 v4.0
- 旧白皮书 v3.4 由用户自行归档备份

### 状态
🧬 已合并

---

## [2026-08-25] 变异：v1.0 DNA 生长方法论定稿

### 触发条件
原白皮书 v3.4 膨胀至约 2 万字，难以在 AI 协作会话中完整加载，导致注意力分散、原则稀释、修宪门槛模糊。

### 生存检查（4问）

1. **DNA 冲突？** 否。方法论自身践行"极致节能"（≤3000 tokens 加载量）、"记忆不可篡改"（只追加不修改）、"向死而生"（DEPRECATE 凋亡清单）。

2. **接口污染？** 否。新增文件均为 Markdown，不触碰任何 gRPC/Protobuf 契约。硬冻结区契约定义位置已明确标注。

3. **版本规则？** 方法论版本 v1.0，与 Helix-Mind 白皮书 v3.4 解耦。方法论管理白皮书，白皮书定义工程实现。

4. **废弃路径？** 无。原白皮书内容拆分为 spec 分卷，无功能退役。

### 影响边界
- **直接影响**：项目文档架构、AI 协作加载协议、修宪流程
- **不触碰**：HelixQuery 接口、SA-Core 引擎实现、联邦协议

### 状态
🧬 已合并

---

## [2026-06-24] 变异：v3.4 SA-Core 引擎 + 突触切断 + 数据库独占所有权

### 触发条件
运行时 LLM 寻路成本过高；用户删除权与不可篡改冲突；多进程文件锁竞争。

### 生存检查（4问）

1. **DNA 冲突？** 否。SA-Core 落实"极致节能"；突触切断落实"记忆不可篡改"；独占所有权强化"最小权限"。

2. **接口污染？** `HelixQueryResult` 新增 `activation_vector`——硬冻结区扩展，向后兼容。

3. **版本规则？** 主干开发 v3.4。

4. **废弃路径？** 禁用 SA-Core → 恢复 v3.3 检索 → `activation_vector` 置空数组。

### 影响边界
- **直接影响**：SA-Core、forgetting、HelixQuery
- **不触碰**：联邦协议、基因锁物理形态、命名规则

### 状态
🧬 已合并

