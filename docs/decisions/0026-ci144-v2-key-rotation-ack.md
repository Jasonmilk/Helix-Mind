# ADR-0026：CI-144 v2.0 密钥轮换期间帧处理 + ACK 超时机制

## 状态
**Draft**（2026-08-29，Alpha 实现期间待锁定）

## 上下文
发送 KEY_ROTATION 帧后、接收端确认前，数据帧的处理方式（旧密钥 or 新密钥）和 ACK 超时机制需明确定义。

## 决策（初步定案，Alpha 编码前最终确认）
- **轮换期间帧处理**：发送轮换帧后，等待 1 个 ACK 确认再切换；期间数据帧仍用旧密钥，避免丢包
- **ACK 超时机制**：超时 100ms 重试，最多 3 次，仍失败则触发告警并回退
- 理由：等待 ACK 避免丢包，旧密钥保证连续性；100ms 超时 + 3 次重试是合理的可靠性机制

## 后果
- 需实现 ACK 帧格式和超时重试机制
- 需定义回退流程（3 次失败后如何处理）
- 需定义告警机制（轮换失败时通知谁）

## 关联
- CI-144 v2.0 升级计划（docs/vision/ci-144-v2.0-upgrade.md）
- ADR-0025（KEY_ROTATION 帧格式）
- ADR-0027（主密钥管理）
