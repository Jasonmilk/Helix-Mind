# 跨仓提案 · anaphase：工具可用性与事件时刻（**工具链现已可用；两条隐患待修**）

> 2026-09-16 由 Cellrix 侧提出。**证据全部是代码行与实际事件文件**，不是推断。  
> 目的：让「模型为什么不调工具」与「时间戳代表什么」各自有一个**可判定**的答案。

## 本提案请求**只有 4 条**（其余全部是留档证据，不是请求）

| 编号 | 请求 | 归属 |
|---|---|---|
| **P0-A** | **缺失必须留痕**：tools 块在/不在 + 原因，写成**事件字段**（Cellrix 只显示，不推导） | anaphase |
| **P0-B** | **工具名与来源在格式上可区分**（P0-C 是它的旧编号，**同一条**） | anaphase |
| **P0-D-1** | **未执行的调用不得成为交付物**（fail-closed） | anaphase |
| **P0-D-2** | **契约单一来源**：指令与解析器就同一事实给同一答案 | anaphase |

> **证据区**（下文的「被推翻假设」表、§二 / §二补、§三、§四、§五、§六、§八、§九）
> 记录的是**排查过程与实测**，用于防止重复排查；**不是请求，也不需要认领**。
> 请求与证据混在一起，正是本文件早先自相矛盾的土壤。

## 一、结论（已由实测取代此前的推断）

**用户报「Tentacle / 搜索不可用」。根因**分层**（见下），且都在我们这一侧 —— 不在 tentacle、不在模型。**

### 结论先行（**三层，别混在一起**）：

**① 用户能看到的失败（不会用 Tentacle / 不会用搜索）—— 真凶 = 测试壳未加载插件，已修并已重放验证。**
`Cellrix/web/tests/start-panel.sh` 从出生起（`989dd00`，2026-09-15 04:55）就没带 `--plugins-dir`，
pickaxe 证实该字符串在它里面从未出现过。**用户跑的正是这个壳**（最初被告知去用的那条路）。
⇒ **「真实路径 `up` 没坏」与「用户因此受害」同时成立** —— 不要读成「这事儿已经自己好了、不用管」。

**② 注入格式把「来源分组」与「工具名」并列 —— 是隐患，不是回归边界（已被日期否掉）。**
`93ecaca`（**2026-09-09 05:31**）引入 `tentacle·fixture: calc(…) | numbers(…)`，
而**最后一次成功调用是 2026-09-13 20:04**（产出裸名 `{"tool":"calc"}`）⇒
**组前缀已存在 4 天而调用照旧成功** ⇒ 它只是「有时会绊倒模型」（本轮实测到一次）。见 P0-B。

**③ 解析器与指令自相矛盾 —— 未执行的调用成了交付物。** 见 §七（P0-D）。

### 第二层（**隐患，非边界**；原判「这才是本回归的病因」已被日期否掉，保留此节作证据）

**决定性实验（2026-09-16，用与 `up` 相同的配置：带插件）**：  
一次真实对话，模型**确实发出了调用**，但它是：

```json
{"calls":[{"tool":"tentacle·fixture","name":"calc","args":{"expression":"8**6"},"expect":"ok"}]}
```

—— 模型把**组标签**当成了工具名。而该 period 里**只有 `attempt` 与 `turn/end`**，  
**没有 `tool/call`，也没有 `tool/result`** ⇒ **调用发出而未被识别 ⇒ 未执行。**

⇒ 这解释了「以前可以、现在不行」：`2026-09-13 20:04` 成功那次，模型产出的是  
`{"calls":[{"tool":"calc",…}]}`（`run-30fabc23294f31e6` 实证）——  
**即分组前缀（`组标签: …`）是后来加的**，加完之后工具名就被组名顶掉了。

### 待改的那一行

修复后第一句真实调用暴露出格式缺陷：

```json
{"calls":[{"tool":"tentacle·fixture","name":"calc","args":{"expression":"8**6"},"expect":"ok"}]}
```

`main.rs:1034-1040` 每行是 `组标签: 工具A(签名): 描述 | 工具B(签名): 描述`。  
模型因此把**组标签**读成工具名。**工具名与来源必须在格式上可区分**（例如每行一个工具，  
来源放行尾括注，或改为 `calc(expression): 描述 [tentacle·fixture]`）。

### 被推翻的三条假设（留档，防止再走一遍）

| 假设                    | 判定 | 证据                                                                                                                                                    |
| --------------------- | -- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| 「tentacle 挂了 / 服务不可用」 | ❌  | 服务一直能起来                                                                                                                                               |
| 「换模型是回归根因」            | ❌  | 最后一次成功调用（`run-30fabc23294f31e6`，`2026-09-13 20:04`）的 `model` **就是 `agnes-2.5-flash`**；且历史 98 个 period 中 `tool/call` **共 24 次**、每次都伴随合法的 `{"calls":…}` |
| 「文本契约在新模型上失效」         | ❌  | 契约在 agnes 上被遵守过，2026-09-16 的复现里也立即被遵守                                                                                                                 |
| 「启动脚本没加载插件是病因」 | ✅ **成立**（它就是用户能看到的失败的真凶）：`start-panel.sh` 从出生起（09-15 04:55）就没带 `--plugins-dir`，而**用户跑的正是它**；已修并重放验证 | — |

**回归窗口**：~~与启动脚本是否带插件一致~~ —— **已关闭**：壳（09-15）晚于沉默起点（09-13），且三个工具停在不同日期 ⇒ 见 §九，**不再追查**。
（旧因果已撤销：壳生于 09-15 04:55，晚于沉默起点 09-13 20:11，**不构成该窗口的解释**。）

## 二、仍请 anaphase 做的两件事（与本提案同源，一并提）

### P0-A：**缺失必须留痕**（本提案的核心，与第一层的教训直接对应）

`main.rs:1003-1010` 是四层嵌套条件：**未配端点 / 连不上 / 列表为空** ——  
三种失败产生**同一个可观测结果**：prompt 里没有这一段。于是「模型说没有工具」  
与「系统忘了给」**完全同形**，这一个缺陷让上面那条排查走了十轮。

**请求**：把 `list_tools()` 的每一种结果写成**事件字段**（例如并进 `context/inject` 的 payload：  
`tools: {status, count, endpoint, reason}`）。**Cellrix 只显示，不推导** ——  
否则将来一定有人在视图层去猜（读 prompt 或另拉 endpoint），那又是一份推导。

**判据**：跑一轮后，**从事件流本身**能读到「本轮 tools 块：在 / 不在（原因）」。

### P0-B：工具名/来源的格式（见上「第二层」）

**判据**：同一句「用 calc 计算 8 的 6 次方」，模型产出的 `tool` 字段等于 `calc`  
（不是组标签），且 `tool/result.ok == true`。

## 三、事件时刻（P1，本侧已修，不阻塞）

`session_events.rs:282 fn ts()` 逐事件生成、秒精度；同一轮的 `think/attempt/reply`  
常共享同秒，而**请求侧**与**响应侧**之间的间隔**就是**该次调用时长。  
Cellrix 侧已把 gap 读在**结束于本行**的那一端：THINK `[0,0,0] → [5000,3000,5000]`，  
`LLM TIME` `— → 13.00s`；并已把列改名 **`Wait`** 并在文档里声明  
「模型行上它是整次调用，think 与 reply 由同一次调用产出，不可分解」。

**仍可选做（低优先）**：毫秒精度（把同批写入的事件按真实间隔分开）。

## 四、本提案**不**要求

- 不改 `http_reasoning.rs` 的请求形状，不引入 API tool-calling（那是另一个决策）。
- 不要求 Cellrix 侧再改任何东西 ⇒ **P1-1 / P1-2 已移出本提案**，
  落到本侧自己的待办：`helix-mind/docs/helixECO/HANDOFF-0916.md` 的「本侧待办」
  （它们不需要 anaphase 裁决，不该卡在别人的排期上）。

---

*登记 ≠ 认领。是否做、何时做，由 anaphase 侧裁决。本文件只固定**问题、证据与判据**。*

## 二补：**组前缀不是回归边界**（已用日期否掉，留档）

曾有一个很自然的假设：注入格式由「裸名」改成「组标签: 名字 | 名字」是"以前可以、现在不行"的原因。  
**代码史否掉了它**：

| 提交                                                                 | 日期                   | 引入                                                 |
| ------------------------------------------------------------------ | -------------------- | -------------------------------------------------- |
| `b06fd25` `feat(ADR-0025): … tool-chain closure … calc/web_search` | 2026-09-07 22:09     | **工具块本身**                                          |
| `93ecaca` `fix(criteria): … L1 schema-driven tiering`              | **2026-09-09 05:31** | **组前缀**（`tentacle·fixture:` 与 `tentacle: {}` 同批出现） |

**而最后一次成功调用是 2026-09-13 20:04**（`run-30fabc23294f31e6`，产出裸名 `{"tool":"calc"}`）。  
⇒ 组前缀**已经存在了 4 天**，那几次仍然成功 ⇒ **前缀不是边界，只是"有时会绊倒模型"的隐患**。

### 那么边界在哪？（未定，但已收窄到两条）

1. **`config.toml` 的 mtime = 2026-09-14 19:35** —— 恰在最后一次成功调用之后。  
   但该文件被 `b4dc907`（`chore(sec): untrack deployment config`）**取消跟踪**，  
   **无法 diff**：只能确认它**现在**含 `tentacle_endpoint = "http://127.0.0.1:50051"`。
2. **模型自述不可作为证据**：同一份 prompt 下，模型对"我有没有工具"的回答并不稳定  
   （本轮在我的复现里它误报组名，在用户的复现里它用裸名成功）。

### 现状（2026-09-16 17:11 实测）

**工具链现在是通的**：用户环境一次真实对话产出  
`{"calls":[{"tool":"calc","args":{"expression":"2**10"}}]}` → `tool/call calc` →  
`tool/result {"ok":true,"result":"1024","duration_ms":595}`，`verdict: Met`，`success: true`。  
⇒ 那条"4 天沉默"**已不再复现**；它要么是 09-14 前后的一次配置/进程状态，要么是模型侧的偶发。

### P0-C（**与 P0-B 是同一条**；编号保留作索引，避免下一个人以为有两条）

`main.rs:1034-1040` 每行 `组标签: 工具A(签名): 描述 | 工具B(签名): 描述`。  
组标签与工具名**并列且无分隔语义**，模型会拿它当工具名（本轮实测到一次）。  
**请求**：让「名字」与「命名空间」在格式上不可混——每行一个工具，来源放行尾括注，  
或明确「`·` 只是命名空间分隔符，工具名是冒号前的第一个标识符」。  
**判据**：同一句「用 calc 计算 8 的 6 次方」连续 5 次，`tool` 字段 5 次都等于 `calc`。

### P0-A 未变（且这次的排查过程就是它的证据）

三种失败（未配端点 / 连不上 / 列表为空）产生同一个可观测结果。**在它被修好之前，  
任何"模型说它没有工具"的报告都无法与"系统忘了给"区分开** —— 上面那 4 天沉默至今无法定性的原因就是这个。

## 五、搜索：**已验**（注册齐全，现在能跑）+ 新发现：**工具循环只走一跳**

### 5.1 三条判据都取到了

| 判据                      | 结果                                                                                                                       |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| 工具注册                    | `helix-tentacle/fixtures/` 含 `calc` / `numbers` / `rate` / `weather` / **`web_search`**（各含 `.manifest.json`）⇒ **注册齐全**   |
| 历史调用                    | `calc` ×18（末次 09-13 20:04）／`weather` ×4（末次 09-08 21:28）／**`web_search` ×2（末次 09-08 21:11，当时 `tool/result ok · 1407ms`）** |
| 现状（2026-09-16 17:4x 实测） | 问「今天北京天气，用 web_search」⇒ `tool/call web_search` → `tool/result {"ok":true,"results":[…]} · 1071ms`，两道 check 均 PASS        |

⇒ **搜索不是"从未可用"，也不是"没注册"**；三个工具**停在不同日期**，这个形状指向"调用发生与否"而不是"某工具坏了"。

### 5.2 新发现（本次实测）：**二次调用不执行，且以 JSON 形式当成回复**

同一轮的 `turn/end.reply` 是：

```
网络搜索的结果与天气无关，我使用天气工具来查询。
{"calls":[{"tool":"weather","args":{"city":"Beijing"},"expect":"ok"}]}
```

**第一次调用执行了，第二次没有** —— 它被当成"回复"写进了交付物。用户因此看到一段 JSON 而不是答案。  
⇒ **工具链是"打一下"而不是"循环"**。这解释了用户报的第二种形态（"该用工具却没用"）的一部分：  
模型想连续用两次时，第二步永远不会发生。

**请求**：明确工具循环的**次数上限与其可见性**。若上限是 1，那么"超出上限的第二次调用"  
必须**不是**回复的正文（否则等于把内部意图当交付物）；要么执行，要么显式声明"已达上限"。

**判据**：同一问连续 3 次，`turn/end.reply` 都不含 `{"calls"` 子串，且 `tool/call` 的次数 ≥ 1。

### 5.3 间歇性 —— **已撤销**（是干净的 before / after）

先前把「三轮失败 + 两轮成功」读成间歇性。**两条历史失败问句重放后撤销该结论**（见 §六）：
同环境、同模型、同样的问句，现在是**带真实工具调用的成功**。

⇒ **不是间歇，是修好前后。** 因此 **P0-A（每轮留痕）回到「应当做」**（它仍能防止下一次同类排查走十轮），
**不是「唯一手段」**。**别把一个已解决的问题写成未解之谜** —— 那会让对方按错误优先级排期。


### 5.4 附加建议：让配置的**结构**可 diff

`config.toml` 被 `b4dc907`（no secrets in git）取消跟踪 ⇒ **配置变更不可审计**，  
而本次回归边界恰好落在配置上（mtime `2026-09-14 19:35`）。

**建议**：提交一份 `config.example.toml`（**只留键名与结构，值全空**）。  
键的新增/删除/改名即可 diff，值仍不入库 —— 成本极低，能防下一次同类事故。

## 六、重放：两条历史失败问句（2026-09-16 17:54，修好的路径）

| 问句（用户当时的失败案例） | 当时 | 现在 |
|---|---|---|
| 「请用 calc 计算 **4 的 23 次方**，不要口算」 | ❌ 把手写的 `pow(4,23)` 当回答输出，无工具调用 | ✅ `tool/call calc` → `result ok` → 答 **70368744177664**（4²³ 正确） |
| 「今天北京天气」 | ❌ 回答「我无法获取实时天气数据」 | ✅ `tool/call weather` → `result ok` → **烟霾 / 31°C / 湿度 33% / 风 13km/h** |

两轮的 `turn/end.reply` **都不含** `{"calls"` ⇒ 一跳循环**未触发**（这两轮只需一次调用）。

⇒ 合并结论：**用户诉求 #1（不会用 Tentacle）与 #2（不会用搜索）在我能产生的运行里都已恢复**；
历史那一段沉默**至少有一部分**没有解释（§二·补），这条要分开说，别混进"已修"。

## 七、P0-D：判定结果 = **解析器与指令自相矛盾**（不是循环上限）

**已判定（读代码，非推断）**：`contract/mod.rs::parse_reasoning_output` 的第一条路径是

```rust
let json_err = match serde_json::from_str::<serde_json::Value>(response) { ... };
parse_tool_fence(response).ok_or_else(|| format!("invalid JSON from LLM: {json_err}"))
```

⇒ **它要求整段回复本身就是合法 JSON**；回退路径只认 tool 围栏。
实测那段回复是**散文 + JSON**（`网络搜索的结果与天气无关…` 之后才是 `{"calls":…}`）⇒ 解析失败
⇒ `run_cycle.rs:1113 warn!("[Reasoning] Unstructured output (no calls plan)")` ⇒ **原文被当成最终回复发出**。

**⇒ 根因不是「循环上限=1」，而是指令与解析器互相矛盾：**
注入的是 `end your reply with ONLY: {...}`（**「以…结尾」在语义上允许前缀**），
解析器要的却是「整段就是它」。**一处事实两个来源，就在这一句话上。**

**拆为两条独立请求**：

- **P0-D-1（fail-closed，现在就可提）**：**解析不出来的内部意图绝不能成为用户可见的答案。**
  观测到的形态是把 `{"calls":…}` 原文交给用户 —— 那是把「内部意图」当「交付物」。
- **P0-D-2（已判定，可提）—— 按「唯一事实来源」提，不提「二选一」**：
  根因是**同一个契约有两份表述**（指令文本 vs 解析器）。请求应为：
  **契约常量定义一处，指令由它渲染、解析器按它解析。**
  只改措辞不消除两份表述，下次改一处仍会漂移。
  **不要**提「支持多跳」—— 那是过度设计，实际没有观察到循环计数。

触发条件（原记录保留）：**一轮里模型想要 ≥2 次调用**时。

触发条件已精确：**一轮里模型想要 ≥2 次调用**时。

实测（2026-09-16 17:4x，问天气）：第一次 `web_search` 执行了（1071ms，有真结果），
模型判断结果不相关、想再调 `weather` —— 那次调用**没有执行**，而是被写进 `turn/end.reply`，
**用户看到的答案是 JSON**。

**请求（只在 anaphase 侧修）**：第二次调用要么执行，要么显式声明「已达调用上限」。
**未执行的调用不得成为回复正文**——那等于把内部意图当交付物。

**明确不要在本侧做的事**：Cellrix 把 reply 里的 `{"calls":…}` 藏起来是**"渲染出来再藏"**，
正是本轮已消灭的形态。

**判据**：同一问连续 3 次，`turn/end.reply` 不含 `{"calls"` 子串，且 `tool/call` 次数 ≥ 1。

## 八、两条工程建议

### P1-1 两个启动脚本 = 同一关注点两份实现

`Cellrix/web/src/bin/up.rs` 与 `Cellrix/web/tests/start-panel.sh` **各自知道"怎么起栈"**。
今天全天在治的病（一份事实两个来源），换到启动层又长了一遍：
`subscribers` vs `targets` 已治，**`up` vs `start-panel.sh` 未治**。
**建议**：`start-panel.sh` 内部改调 `up`（或两者共用同一个函数），只留一个知情人。
否则下次再加一个参数，还会再漏一次 —— 而这次已经为此付了十轮排查。

### P1-2 能力断言要**运行期**，不是静态 grep

今天的守卫是"壳的文本里必须有 `--plugins-dir`"（文本断言）。它挡得住"参数丢了"，
挡不住"插件目录空了 / 路径挂错 / manifest 坏了"。
**建议**：live 检查时断言 **`list_tools()` 返回 > 0**（断言"能做"，不是"在听"）。

## 九、**不再追查** 09-13 起的沉默窗口（附理由）

曾把它当成一段未定性的回归。**证据表明它不是一件事**：

| 工具 | 末次成功调用 |
|---|---|
| `calc` | 2026-09-13 20:04 |
| `weather` | 2026-09-08 21:28 |
| `web_search` | 2026-09-08 21:11 |

**三个工具停在不同日期** ⇒ **配置断裂会让所有工具同时停**；停在不同日期是**使用驱动**
（后来没人问天气/搜索了），不是系统故障。
⇒ 那个「沉默窗口」很可能是 **artifact（没人问需要工具的问题）+ 09-16 那几轮真失败** 的混合体。
真失败只有 08:30 三轮（`4^23 别口算`、`天气`），**真凶 = 测试壳，已修并重放验证**。

⇒ **本项降级为「不再追查」，优先级归零**。**这是本轮最后一个还能再烧十轮的口子，就此关闭。**

## 十、**根因已判定**：`expect` 是必填字段，而模型会漏写它（用户第二条诉求）

**证据（同一条链、同一进程内）**：

| `assistant/attempt` 的原文 | 结果 |
|---|---|
| `{"calls":[{"tool":"weather","args":{"city":"广州"},"expect":"ok"}]}` | ✅ 派发、执行 1916ms、两道 check 通过 |
| `{"calls":[{"tool":"web_search","args":{"max":10,"q":"…"}}]}` ×3 | ❌ **未派发**，原文成为交付物 |

**代码**：`contract/mod.rs`

```rust
pub struct Call { tool: String, args: BTreeMap<String, serde_json::Value>, expect: Expect }
pub enum Expect { Numbers, Rate, Text, Ok }     // 无 Default，字段无 #[serde(default)]
```

⇒ **`expect` 必填且无默认** ⇒ 模型漏写时 `serde_json::from_value::<Vec<Call>>` 报 `missing field
expect` ⇒ `parse_reasoning_output` 的两条路径都不成立 ⇒ `warn!("Unstructured output")` ⇒ **原文即答案**。

**请求（一行级）**：给 `expect` 加 `#[serde(default)]`（并 `impl Default for Expect { Ok }`），
与注入指令里那句 `"expect":"ok"` 一致。**或**把指令改成"缺 `expect` 视为 ok"。
**判据**：连 3 次「用 web_search 搜一个词」，3 次都出现 `tool/call web_search`。

**注意**：这与 P0-D-1（未执行的意图不得成为交付物）是**两条独立缺陷** ——
即使补上默认值，解析失败时的泄漏仍会发生。

## 十一、P0-E 记忆与当前对话**必须可区分**（新，跨仓）

实测（用户 8 轮导出）：Turn 4 的 USER 行原文是「**我还没说什么项目你就回答了?**」——
用户自述从未问过那个项目；而 Turn 6 的 THINK 自述它回忆到
「**选品三原则 / 获客渠道**的学习内容」与「一个 **previous episode** 提到 GitHub 项目 Lumtract」。

⇒ **别场会话的 episodic memory 被注入 CONTEXT，模型把它当成了本轮对话的历史**，于是回答了一个
根本没被问的问题。**没有 THINK 与 CONTEXT 的 payload 全文，这个缺陷永远只表现为"模型偶尔犯傻"。**

**请求**：注入的 episodic memory 在**结构上**与本轮对话可区分（不同通道 / 不同标记），
使模型（与审查者）能分辨「我记得的」与「你刚说的」。
**判据**：本轮 USER 行的原文能在事件流里被逐字回溯；模型不得把 injection 当作 user turn。

## 十二、P0-F 无判据的轮次**不得** `success=true`（新，跨仓）

实测：Turn 5 / 7 / 8 的 `turn/end` 都是 `done=true · success=true`，而这三轮
**没有 `check/status`、没有 `verdict/status`，交付物是 JSON**。

⇒ `success=true` 在**零判据**下成立 ⇒ 它是"没崩溃"，不是"成功了"，与 `answer.delivered`
那道 check 的语义直接冲突。
**请求**：无 verdict 的轮次，`success` 不得为 true（或把该字段改名为 `completed`）。
